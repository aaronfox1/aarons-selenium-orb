description: >
  This command executes a bash script which curls and then runs Selenium. 
  The background: true, steps allows Selenium to continue running in the background 
  while we execute our tests.

steps:
      - run:
          name: Run Selenium
          command: sh scripts/run_selenium.sh
          background: true